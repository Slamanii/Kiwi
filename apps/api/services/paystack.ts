import { BankDetailsInput } from '@kiwi/types'

export async function verifyAccountWithPaystack(accountNumber: string, bankCode: string) {
    
    const success = 'done'
    
    return success
}

export async function createPaystackRecipient(data: BankDetailsInput) {
    
}